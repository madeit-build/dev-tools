[project]
name = "madeit-log"
version = "0.1.0"
description = "Made I.T. structured logging, the Python mirror of @madeit-build/log"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["jsonschema>=4.23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["madeit_log"]
